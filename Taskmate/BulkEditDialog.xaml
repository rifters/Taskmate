<Window x:Class="Taskmate.BulkEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bulk Edit Tasks" Height="400" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="16" FontWeight="Bold" Margin="0,0,0,5">
            <Run Text="Bulk Edit "/>
            <Run x:Name="txtCount" Text="0"/>
            <Run Text=" Selected Tasks"/>
        </TextBlock>
        <TextBlock Grid.Row="1" Text="Choose which properties to apply to all selected tasks:" FontSize="12" Opacity="0.7" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="2">
            <!-- Category -->
            <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="15" Margin="0,0,0,10" CornerRadius="5">
                <StackPanel>
                    <CheckBox x:Name="chkCategory" Content="Set Category" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <ComboBox x:Name="cmbCategory" Height="30" IsEnabled="{Binding IsChecked, ElementName=chkCategory}"/>
                </StackPanel>
            </Border>

            <!-- Difficulty -->
            <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="15" Margin="0,0,0,10" CornerRadius="5">
                <StackPanel>
                    <CheckBox x:Name="chkDifficulty" Content="Set Difficulty" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <ComboBox x:Name="cmbDifficulty" Height="30" IsEnabled="{Binding IsChecked, ElementName=chkDifficulty}">
                        <ComboBoxItem Content="Easy (1)"/>
                        <ComboBoxItem Content="Medium (2)" IsSelected="True"/>
                        <ComboBoxItem Content="Hard (3)"/>
                    </ComboBox>
                </StackPanel>
            </Border>

            <!-- Time Estimate -->
            <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="15" Margin="0,0,0,10" CornerRadius="5">
                <StackPanel>
                    <CheckBox x:Name="chkTime" Content="Set Time Estimate (minutes)" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <TextBox x:Name="txtTime" Height="30" Text="15" VerticalContentAlignment="Center" IsEnabled="{Binding IsChecked, ElementName=chkTime}"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Apply" Width="100" Height="35" Margin="0,0,10,0" Click="btnApply_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="100" Height="35" Click="btnCancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>