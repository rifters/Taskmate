<Window x:Class="Taskmate.ListManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="List Manager" Height="500" Width="600"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Add New Item" Grid.Row="0" Margin="0,0,0,10" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="txtNewItem" Grid.Column="0" Margin="0,0,10,0" 
                         VerticalContentAlignment="Center" Height="32"
                         FontSize="14"/>
                <Button x:Name="btnAdd" Grid.Column="1" Content="Add" Width="90" Height="32"
                        Click="btnAdd_Click" FontWeight="Bold"/>
            </Grid>
        </GroupBox>

        <GroupBox Header="Items" Grid.Row="1" Margin="0,0,0,10" Padding="10">
            <ListBox x:Name="lstItems" 
                     SelectionMode="Single"
                     FontSize="13"
                     BorderThickness="1"/>
        </GroupBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
            <Button x:Name="btnRemove" Content="Remove Selected" Width="130" Height="35" Margin="5" Click="btnRemove_Click" FontSize="13"/>
            <Button x:Name="btnClear" Content="Clear All" Width="130" Height="35" Margin="5" Click="btnClear_Click" FontSize="13"/>
            <Button x:Name="btnImport" Content="Import..." Width="130" Height="35" Margin="5" Click="btnImport_Click" FontSize="13"/>
            <Button x:Name="btnExport" Content="Export..." Width="130" Height="35" Margin="5" Click="btnExport_Click" FontSize="13"/>
        </StackPanel>

        <Grid Grid.Row="3" Margin="0,5,0,0">
            <TextBlock x:Name="txtCount" HorizontalAlignment="Left" VerticalAlignment="Center" FontWeight="Bold" FontSize="13"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnSave" Content="Save" Width="100" Height="35" Margin="5" Click="btnSave_Click" IsDefault="True" FontWeight="Bold"/>
                <Button x:Name="btnCancel" Content="Cancel" Width="100" Height="35" Margin="5" Click="btnCancel_Click" IsCancel="True"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>